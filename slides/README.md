# Slides

This folder contains the LaTeX beamer presentation for the project.

## Files

- `main.tex`: slide deck for the AlexNet vs DHVT comparison

## Compile

From the project root:

```powershell
cd slides
pdflatex main.tex
pdflatex main.tex
```

The slide deck reuses the exported figures stored in `../report/figures/`.
